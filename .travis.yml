anguage: node_js

node_js: stable

cache:
  directories:
  - node_modules
before_install:

  - npm install -g hexo-cli

install:
  - npm install

script:
  - hexo g 

after_script:
  - cd ./public
  - git init
  - git config user.name "sh"
  - git config user.email "2029403020@qq.com"
  - git add .
  - git commit -m "TravisCI 自动部署"
  # Github Pages
  - git push --force --quiet "https://4XPGivDCnrQp7vfo4tIn3ZvbqHVUn9ygTP062wcL@${GH_REF}" master:master

env:
  global:
    - GH_REF: github.com/yuexiliuli/yuexiliuli.github.io.git
  
notifications:
  email:
    - 2029403020@qq.com
  on_success: change
  on_failure: always
