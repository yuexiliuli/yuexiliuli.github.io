language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install

#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "yuexiliuli" --{GitHub账户名称}
  - git config user.email "2029403020@qq.com" --{Github账户邮箱}
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - blog-source --{Hexo源码分支名称}
env:
 global:
   - GH_REF: github.com/yuexiliuli/yuexiliuli.github.io.git --{仓库地址}